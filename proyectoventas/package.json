{
  "name": "marketplace-seguro",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "api:dev": "npm --workspace backend run start:dev",
    "web:dev": "npm --workspace frontend run start",
    "api:build": "npm --workspace backend run build",
    "web:build": "npm --workspace frontend run build",
    "build": "npm run api:build && npm run web:build"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
